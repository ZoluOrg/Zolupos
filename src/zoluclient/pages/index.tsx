import type { NextPage } from "next";
import Head from "next/head";
import Image from "next/image";
import { Button } from "../components/Button";

const Home: NextPage = () => {
  return (
    <div className="">
      <Button Color="mallow">button</Button>
    </div>
  );
};

export default Home;
